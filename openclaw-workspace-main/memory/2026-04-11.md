
## jj (jjj-easynote-tk11bz-1) - packy2 machine

**SSH**: `sshpass -p 'zxcv1234' ssh -p 2222 hkirk@100.103.12.85` (local network only, Tailscale SSH not set up)

**Hardware**: AMD C-50 (dual-core 1GHz), 1.6GB RAM, 458GB disk

**GPT2 Fix Applied** (2026-04-11 22:40 UTC):
- **Problem**: `bfloat16` causes segfault on AMD C-50 (incompatible CPU architecture)
- **File**: `~/packy2/gpt2_mouth.py`
- **Original**: `_MODEL = GPT2LMHeadModel.from_pretrained(MODEL_DIR, torch_dtype=torch.bfloat16).eval()`
- **Fixed**: `_MODEL = GPT2LMHeadModel.from_pretrained(MODEL_DIR).eval()` (float32)
- **Backup**: `~/packy2/gpt2_mouth.py.bak`
- **Result**: GPT2 generates ~0.5s/token (slow but functional)

**Packy2 project**: `~/packy2/` - homebrew AI assistant with GPT2-small (124M params), homebrew orchestration layer in `layer/`

**TODO**: Media server setup still pending - Tailscale SSH needs to be enabled on jj first.

## Discord Multi-Agent Breakdown (2026-04-12 ~02:25 UTC)

**What happened**: Henrik set up multi-agentic Discord communication. I (Zoe) tried to route 3 agents through myself and broke the setup. The gateway got stuck in a broken state with bloated memory (523MB+), multiple gateway processes, and a frozen Discord session.

**Fix**: Full gateway reboot - killed all openclaw/gateway processes and restarted fresh.

**Current state after reboot**:
- Gateway: restarted, RAM back to normal (~70MB)
- Discord: OK (token config valid, accounts 1/1)
- Telegram: OK
- Discord session `b2d68802-...` was 9 min stale but reconnects cleanly
- Tailscale: joined Henrik's tailnet (100.71.199.20)
- jjj (jjj-easynote-tk11bz-1): Tailscale SSH working

**Reminder**: One gateway instance per install. Don't run multiple gateways or route multiple agents through the main agent.
