{
  "name": "@openclaw/summarizer",
  "version": "0.1.0",
  "private": true,
  "description": "Agent summary ingestion, normalization, and validation services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.22.4",
    "@openclaw/core-types": "workspace:*",
    "@openclaw/core-schemas": "workspace:*",
    "@openclaw/core-events": "workspace:*",
    "@openclaw/core-logging": "workspace:*",
    "@openclaw/core-errors": "workspace:*",
    "@openclaw/agent-protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^1.2.1"
  }
}
