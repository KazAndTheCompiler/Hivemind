{
  "name": "@secdev/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical TypeScript types for the SecDev Labs monorepo",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^1.2.1"
  }
}
