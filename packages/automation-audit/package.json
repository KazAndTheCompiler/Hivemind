{
  "name": "@openclaw/automation-audit",
  "version": "0.1.0",
  "private": true,
  "description": "Audit event persistence for automation enforcement",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@openclaw/automation-core": "workspace:*",
    "@openclaw/core-logging": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^1.2.1"
  }
}