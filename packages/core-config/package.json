{
  "name": "@openclaw/core-config",
  "version": "0.1.0",
  "private": true,
  "description": "Environment-based configuration with Zod validation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.22.4",
    "@openclaw/core-types": "workspace:*",
    "@openclaw/core-schemas": "workspace:*",
    "@openclaw/core-errors": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.11.0",
    "vitest": "^1.2.1"
  }
}
